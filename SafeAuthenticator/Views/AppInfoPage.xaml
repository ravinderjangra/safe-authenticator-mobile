<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:authViewModels="clr-namespace:SafeAuthenticator.ViewModels;assembly=SafeAuthenticator"
             xmlns:behaviour="clr-namespace:SafeAuthenticator.Controls.Behaviour"
             xmlns:controls="clr-namespace:SafeAuthenticator.Controls"
             d:DataContext="{d:DesignInstance Type=authViewModels:AppInfoViewModel, IsDesignTimeCreatable=False}"
             x:Class="SafeAuthenticator.Views.AppInfoPage" 
             Title="App Info"
             Padding="16">

    <ContentPage.Content>
        <StackLayout Spacing="10">

            <Grid Padding="0" 
            Margin="0,15,0,0"
            VerticalOptions="CenterAndExpand" 
            RowSpacing="10" 
            ColumnSpacing="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackLayout Spacing="0"
                VerticalOptions="FillAndExpand"
                HorizontalOptions="FillAndExpand">

                    <Label Text="{Binding AppModelInfo.AppName}"
                    TextColor="Black" 
                    FontSize="Medium"
                    FontAttributes="Bold"/>

                    <Label Text="{Binding AppModelInfo.AppVendor}"
                    TextColor="#8b8b8b" 
                    FontSize="12"/>
                </StackLayout>

                <Button
                Grid.RowSpan="2" 
                Grid.Column="1" 
                Text="REVOKE"
                TextColor="#555555"    
                VerticalOptions="Center"
                HorizontalOptions="End"
                HeightRequest="35"                    
                BackgroundColor="#C2C1C1"
                Command="{Binding RevokeAppCommand}"/>
            </Grid>

            <ListView HasUnevenRows="True" 
                      SeparatorVisibility="None"
                      ItemsSource="{Binding AppModelInfo.Containers}"
                      IsPullToRefreshEnabled="False"
                      CachingStrategy="RecycleElement">
                
                <ListView.Behaviors>
                    <behaviour:ListViewNoSelectionBehavior />
                </ListView.Behaviors>

                <ListView.Header>
                    <Label Text="Permissions granted" 
                           FontSize="14" 
                           TextColor="Black"/>
                </ListView.Header>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <controls:ContainerView/>
                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.Footer>
                    <StackLayout Orientation="Horizontal">
                        <Label Margin="20" 
                               VerticalOptions="CenterAndExpand" 
                               HorizontalOptions="CenterAndExpand"
                               Text="No containers."
                               IsVisible="{Binding AppModelInfo.Containers.Count, Converter={StaticResource IsCollectionEmptyConverter}}" />
                    </StackLayout>
                </ListView.Footer>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
